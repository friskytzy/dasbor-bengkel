# --- Database (Railway PostgreSQL) ---
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# --- Clerk authentication ---
CLERK_SECRET_KEY="sk_test_replace_with_real_secret"
# Note: Publishable key WAJIB valid Base64. Untuk dev/CI yang tidak butuh
# Clerk dashboard, dummy di bawah aman dipakai (decode -> clerk.example.com$).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_Y2xlcmsuZGFzYm9yLWJlbmdrZWwuZXhhbXBsZS5jb20k"
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/dashboard"
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL="/dashboard"

# --- Fonnte WhatsApp gateway ---
FONNTE_API_KEY="your_fonnte_device_token"
FONNTE_API_URL="https://api.fonnte.com/send"

# --- App ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Dasbor Bengkel"

# --- Redis (queue / rate-limit) ---
REDIS_URL="redis://localhost:6379"

# --- Cron auth (used by /api/cron/* and external scheduler) ---
CRON_SECRET="change-me-to-a-long-random-string"

# --- Admin allow-list (Clerk user IDs separated by comma) ---
ADMIN_USER_IDS=""
